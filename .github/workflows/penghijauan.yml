name: titid
on:
  workflow_dispatch:
  schedule:
    - cron: "0 */4 * * *" # tiap jam
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: commit
      run: |
        git config user.name "whatsmynameidontknow"
        git config user.email "akunhantusaya@gmail.com"
        git commit --allow-empty -m "titid"
        git push
